export const environment = {
  production: false,
  apiUrl: "https://localhost:44381/api/tasks",
  webApp: true // More content will be displayed in the web application, encouraging, for example, to download the application to other devices.
};
